
const Gameboard = () => {
    let GameboardArray = 'new Array(10).fill(null)';
return GameboardArray};



















document.querySelectorAll('div').forEach((div,i) => {
    div.id = i;
    div.addEventListener('click', async () => {
    });
});

console.log(Gameboard.GameboardArray);